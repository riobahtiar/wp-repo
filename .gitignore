# =============================================================================
# WP monorepo (wp-content as repo root)
# Track only first-party packages + root tooling. Everything else is host noise
# (stock themes, uploads, third-party plugins, core language packs).
# =============================================================================

# Ignore all by default
/*

# --- Monorepo root (allowlist) ------------------------------------------------
!/.gitignore
!/.gitattributes
!/README.md
!/AGENTS.md
!/CLAUDE.md
!/package.json
!/package-lock.json
!/turbo.json
!/.npmrc
!/.nvmrc
!/.editorconfig
!/.github/

# Workspace install artefacts (even if a parent rule re-includes them)
node_modules/
**/node_modules/
.turbo/
**/.turbo/
npm-debug.log*
yarn-error.log*
.DS_Store

# Nested lockfiles are forbidden — root package-lock.json is the only one.
# (npm workspaces industry default: one lockfile, install from root only.)
plugins/**/package-lock.json
themes/**/package-lock.json

# --- First-party plugins (allowlist) ------------------------------------------
!/plugins/
/plugins/*
!/plugins/wp-bizwit/

# Nested package rules (vendor, build, …) live in each package’s own
# .gitignore — e.g. plugins/wp-bizwit/.gitignore.

# --- Future first-party themes (uncomment when you add one) -------------------
# !/themes/
# /themes/*
# !/themes/<your-theme>/

# --- Never commit WordPress host state ----------------------------------------
# (redundant with /* but explicit for safety if the allowlist grows)
/uploads/
/upgrade/
/cache/
/mu-plugins/
/languages/
